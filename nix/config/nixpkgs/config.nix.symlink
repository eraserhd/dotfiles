{
  packageOverrides = pkgs: with pkgs; {
    myPackages = pkgs.buildEnv {
      name = "my-packages";
      paths = [
        ag
        asciidoc
        autossh
        awscli
        bash-completion
        clojure
        rlwrap
        wget
      ];
    };
  };
}
