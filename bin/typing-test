#!/usr/bin/env bash

readonly TESTS=( $(sed -n 's/^\*://p' <~/src/dotfiles/programs/gtypist/gtypist.typ) )

test=$(( RANDOM % ${#TESTS[@]} ))
testLabel=${TESTS[$test]}
gtypist -i -b -l $testLabel ~/src/dotfiles/programs/gtypist/gtypist.typ
